<div ng-controller='tasksCtrl' ng-init='tasks = <%= @tasks.to_json.html_safe %>; currentTaskTime = <%= @current_task_time.to_json.html_safe %>'>
  <div ng-init="setCurrentTaskTime(currentTaskTime)">
    {{currentTaskTime.taskContent}}
    {{status}}
    {{counter}}
  </div>
  <hr />

  <div ng-view></div>
</div>
