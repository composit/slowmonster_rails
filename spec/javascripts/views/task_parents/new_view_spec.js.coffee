describe 'task parent new view', ->
  describe 'instantiation', ->
    xit 'builds a new task parent object in the collection'
    xit 're-renders when errors change'
    xit 'sets the child task id on the new model'

  describe 'save', ->
    xit 'saves on submit'
    xit 'removes old errors'
    xit 'posts the model attributes'

    describe 'success', ->
      xit 'renders the task parents index'

    describe 'error', ->
      xit 'sets the error on the model'

  describe 'render', ->
    xit 'links the form to the view'

    xit 'sets up autocomplete on the task content'
